# API Reference

## `get_user()`

Returns user data

```python
import requests
response = requests.get('/user')
```

## `create_user()`

Creates new user profile

```python
import requests
response = requests.post('/user')
```